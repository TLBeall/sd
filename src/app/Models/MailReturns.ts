import { Returns } from "./Returns";

export class MailReturns {
    MailCode: string;
    MailDescription: string;
    ListOwner: number;
    ListManager: number;
    Recency: number;
    Measure: Returns;
}